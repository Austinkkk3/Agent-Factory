# Agent Factory — Manager Brief

---

## 一句话说清楚这是什么

我要用 IBM watsonx Orchestrate 从零搭建一套**多智能体 AI 系统**，处理零售企业的发票异常，并内置企业级安全护栏、治理审计和成本追踪。
同时用 Claude Agent SDK 搭建完全相同的系统，做一个**客观的横向技术对比**。
最终交付物是一段**屏幕录制演示视频** + 一个**交互式 HTML 对比页面**。

---

## 我们在解决什么问题

每次做 AI 方案，客户都会问同一批问题：

- 数据会出我的边界吗？
- AI 做了什么决定，谁能审计？
- 以后能换别家的 AI 模型吗？
- 部署在 IBM Cloud 上，能迁到 AWS 或者本地机房吗？

现有的演示都是在**口头回答**这些问题。这个资产让答案**在屏幕上直接看到**。

---

## 业务场景：发票异常处理

选这个场景是因为它治理要求高、混合了规则判断和 AI 推理，是真实的企业痛点。

```
发票进来
  │
  ├─ 第 1 步：验证发票字段、查重           ← 纯规则，不用 AI
  │
  ├─ 第 2 步：与采购订单、收货单三方匹配   ← 纯规则，不用 AI
  │
  ├─ 正常 → 自动过账
  │
  └─ 异常 → AI 分析原因、引用政策、给出处置建议
              │
              └─ 金额超 $10K 或疑似欺诈 → 人工审批
```

**4 种测试场景**（两个平台跑完全相同的数据）：

| 场景 | 说明 | 预期结果 |
|------|------|---------|
| 正常发票 | 金额、数量完全吻合 | 自动过账，AI 参与最少，成本最低 |
| 容差异常 | 单价超出容差 8%（上限 5%） | 转经理人工审批 |
| 重复发票 | 发票号与历史记录重复 | 规则层直接拦截，AI 零参与，成本接近零 |
| 欺诈发票 | 供应商风险标记 + 银行账户刚换 + 金额 10 倍异常 | AI 推理分析 + 强制人工审批 |

---

## 核心亮点：企业级护栏（全程可见）

这些不是"以后再加"——从第一天就内置，演示中在屏幕上直接展示：

| 能力 | 展示什么 |
|------|---------|
| **PII 自动脱敏** | 发票中的姓名、税号、账号在进入 AI 之前被自动屏蔽 |
| **注入攻击拦截** | 在发票字段里写"忽略之前的指令"——系统直接拒绝并记录 |
| **权限控制** | 每个智能体和工具默认禁止访问，必须显式授权 |
| **人工介入** | 超过 $10,000 或疑似欺诈，自动暂停等待人工确认 |
| **完整审计日志** | 每一个 AI 决策和工具调用都以结构化 JSON 记录 |
| **治理证明** | 每条策略都能关联到"它在运行时确实被执行"的日志证据 |
| **无硬编码凭证** | 所有密钥通过 HashiCorp Vault 管理，代码库里零密码 |

---

## 两个平台怎么比较

### 控制变量（两边完全一样）

- 相同的工具函数名和返回格式
- 相同的 4 个测试发票
- 相同的 10+ 评估集（每个都有预期结果）
- 相同的成本字段名（确保数据可以直接对比）

### 对比维度

| 对比点 | 实际测什么 |
|--------|-----------|
| **Token 消耗** | 同一个场景，哪个平台用了更多 token？ |
| **工具调用次数** | 有没有多余的工具调用（每次都要钱）？ |
| **响应延迟** | 每一步耗时多少毫秒？ |
| **单笔成本** | 每处理一张发票实际花多少钱？ |
| **规模成本** | 每天 100 / 500 / 1000 / 5000 张发票，月费各是多少？ |
| **护栏原生程度** | 哪些安全功能是平台内置的？哪些需要自己写代码？ |
| **治理审计** | 原生支持还是靠第三方工具拼出来的？ |
| **可移植性** | 同一套代码能不改地部署到几个环境？ |

### 每笔交易的输出数据（两边格式相同）

```json
{
  "platform": "watsonx_orchestrate",
  "scenario": "fraud",
  "tokens_in": 1240,
  "tokens_out": 310,
  "tool_calls": 4,
  "agent_hops": 3,
  "latency_ms": 2100,
  "cost_usd": 0.0031,
  "hitl_triggered": true
}
```

这份数据同时输出为 CSV，用于最终 HTML 页面的成本计算器。

---

## 构建计划（8 个阶段）

| 阶段 | 做什么 | 关键节点 |
|------|--------|---------|
| **1. 项目基础** | 建 Git 仓库，搭 ADK 脚手架，所有密钥写占位符 | 本地服务器可以启动 |
| **2. 工具层** | 写 7 个工具函数 + mock 数据 | **工具签名在这里锁定**，Claude 那边用相同的 |
| **3. 规则流程** | 发票验证流程 + 三方匹配流程 | 两个无 AI 的确定性管道可以独立运行 |
| **4. AI 智能体** | 异常分类智能体、审批路由智能体、编排器 | 完整的端到端流程跑通 |
| **5. 护栏 & 治理** | PII 脱敏、注入过滤、RBAC、审计日志、策略注册 | 护栏拦截可以在屏幕上演示 |
| **6. 可观测性 & 评估** | 全链路追踪、评估集、基线指标 | 每一步的 token 和成本可以看到 |
| **7. 样本数据 & 成本输出** | 4 个测试发票、成本计算脚本、JSON/CSV 输出 | **样本数据在这里锁定**，两边用同一份 |
| **8. 部署 & 演示脚本** | 部署到 watsonx Orchestrate，验证第二个部署环境，写 DEMO.md | 9 个验收标准全部确认 |

---

## 演示的三个关键时刻

| 时刻 | 操作 | 屏幕上看到什么 |
|------|------|--------------|
| **护栏拦截** | 发送一条含有注入攻击的发票 | 系统拒绝，审计日志立刻出现记录 |
| **治理证明** | 打开治理注册表 | 欺诈策略 → 关联到它实际触发时的日志条目 |
| **完整成本追踪** | 运行欺诈发票，打开 trace_viewer | 每一跳：智能体名称、token 数、延迟、成本，底部显示总计 |

---

## 验收标准

- [ ] 多智能体系统端到端跑通（编排器 + 2 个规则流程 + 2 个 AI 智能体）
- [ ] 护栏拦截在屏幕上可见（PII 脱敏 + 注入攻击被拦）
- [ ] 治理证明可见（策略 ID 关联到运行时审计日志）
- [ ] 完整成本追踪可见（每步 token 数 + 延迟 + 费用）
- [ ] 代码库零硬编码密钥
- [ ] 人工介入在欺诈场景触发
- [ ] 成本数据以 JSON + CSV 输出（单笔 + 规模模型）
- [ ] 4 个测试场景全部以正确结果跑通
- [ ] 同一套代码在 ≥ 2 个部署环境运行（证明可移植性）
